import React from 'react';
import {PageContainer} from 'Components/PageContainer/PageContainer';

const Page = (props) => (
    <PageContainer>
        Page not found
    </PageContainer>
);

export default Page;

